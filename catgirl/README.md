# 🐱 涩涩猫娘奈奈 (1.5B 增强版)

这是一个基于 **Qwen2.5-1.5B-Instruct** 微调的猫娘人格模型。

## 🚀 快速开始

1. **生成数据**
   ```bash
   python step1_create_data.py
   ```

2. **训练模型** (需要约 10-15 分钟)
   ```bash
   python step2_train.py
   ```

3. **开始聊天**
   ```bash
   python step3_chat.py
   ```

## 📝 更新日志

- **v2.0 (1.5B)**: 升级基座模型到 Qwen2.5-1.5B，智能程度大幅提升，对话更流畅，逻辑更清晰。
- **v1.0 (0.5B)**: 初始版本，已备份至 `../catgirl_0.5b`。

## 💡 提示

- 如果显存不足，可以在 `step2_train.py` 中减小 `per_device_train_batch_size` 或 `MAX_LENGTH`。
- 训练数据在 `step1_create_data.py` 中修改。
